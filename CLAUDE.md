# CLAUDE.md

このファイルは、このリポジトリで作業するAIアシスタント（Claude Code等）向けの
ガイドラインです。

## プロジェクトの目的

日本の東証上場中小型株（時価総額100億〜3,000億円、`config/settings.py`で変更可）
から、利益成長に対して株価がまだ十分評価されていない銘柄を毎週自動発掘し、
その後の実績まで長期間検証し続ける個人用投資研究サイト（「投資版miyagi-kids」）。

## 最重要原則: AIとPythonの役割分担

**この原則はこのプロジェクトの根幹であり、絶対に崩してはならない。**

- 株価・財務指標・スコア・ランキング・順位変動・5年シミュレーションなど、
  すべての数値計算は Python (`src/indicators.py`, `scoring.py`, `forecast.py`,
  `ranking.py`, `performance.py`) が行う。
- AI (`src/ai_analysis.py`) は、Pythonの数値スクリーニング・スコアリングが
  完了した後の上位候補についてのみ、「なぜこの会社が面白い可能性があるのか」を
  強気材料・弱気材料・確認事項・成長ドライバー・競争優位性に整理する
  **定性分析の役割だけ**を担う。
- AIにランキングを決めさせない。AIに数値計算をさせない。AIに「買い」
  「売り」「絶対上がる」「5年後に3倍になる」等の断定的な投資判断・株価予測を
  させない。
- 新機能を追加する際、「AIが〜を判断する」という設計になっていないか必ず
  確認すること。判断・計算はPython、説明・整理はAI。

## アーキテクチャ

- 常駐サーバー・バックエンドAPIは存在しない。Python が SQLite (`data/stock.db`)
  を更新し、Jinja2 で静的HTMLを `site/` に生成し、GitHub Pages がそれを配信する。
- `data/stock.db` は **意図的に `.gitignore` に含めていない**。GitHub Actions
  ジョブは使い捨て環境のため、DBをリポジトリにコミットして永続化しない限り
  過去のランキング履歴が毎週消えてしまう。DBファイルをgitignoreに追加する
  変更は原則として行わないこと。
- パイプラインの実行順序は `src/main.py` の `run_pipeline()` を参照:
  データ取得 → 指標計算・スクリーニング → スコアリング → 5年シミュレーション
  → ランキング生成・順位変動記録 → AI分析（キャッシュ考慮） → 実績検証
  → HTML生成。

## データ処理ルール

- 存在しないデータを推測して埋めない。欠損値は必ず `None` として扱い、
  `0` と混同しない（`src/indicators.py` の設計方針）。数値0（利益ゼロ等）と
  「データが取得できなかった」は意味が違う。
- 契約プランや一時的なAPI障害で一部データが取得できなくても、パイプライン
  全体を止めない。例外は `errors` テーブルに記録し、処理を継続する
  （`src/data_loader.py`, `src/jquants_client.py` のtry/exceptパターンを踏襲する）。
- J-Quants APIの仕様を推測でコードに反映しない。仕様変更が疑われる場合は
  公式ドキュメント（https://jpx-jquants.com/ja/spec/ 配下）を確認してから
  実装すること。

## look-ahead bias 防止

- `src/indicators.py` の `build_indicator_snapshot()` は `as_of_date` 以前に
  開示されたデータのみを使用する。過去のランキングを再計算する際にも、
  その時点でまだ発表されていなかった決算・株価を混ぜてはならない。
- `src/performance.py` の実績検証は、`ranking_history` に保存された
  「ランキング作成日時点の価格」を起点にし、対象ホライズン
  （1週/1か月/3か月/6か月/1年）が到来した後にだけ実績株価を埋める。
  未来の情報で過去の行を書き換えない。

## 履歴を削除しないこと

以下のテーブルは過去の行を **絶対に削除・上書きしない**（`INSERT` のみ、
既存の実績データを持つ行への `UPDATE` は慎重に）:

- `ranking_history`（毎週のランキング。予測モデルの検証そのもの）
- `forecast_results`（予測時点のスナップショットと実績リターン）
- `ai_analyses` / `sources`（AI分析の履歴と根拠）
- `updates` / `errors`（実行ログ）

新しい週の実行は必ず新しい `run_id` で行の追加を行う。既存の週のデータを
書き換えるような変更は原則として避けること。

## AI分析のルール

- `src/ai_analysis.py` の `SYSTEM_PROMPT` / `ANALYSIS_TOOL` を変更する場合も、
  上記の役割分担・断定禁止・情報源不明時の扱い（「情報を確認できませんでした」）
  を必ず維持すること。
- 受注・市場シェア・特定顧客・契約内容・将来の設備投資計画など、根拠のない
  具体的事実を書かせない。
- AI分析結果は必ずキャッシュし、`config/settings.py` の
  `AI_REANALYSIS_*` 条件に該当する場合のみ再分析する（無駄なAPI呼び出しの防止）。

## 秘密情報管理

- APIキーをコードに直接書かない。`JQUANTS_API_KEY` / `ANTHROPIC_API_KEY` は
  必ず環境変数（ローカルは `.env`、GitHub Actionsは Secrets）から読む。
- `.env` は `.gitignore` 済み。ログにAPIキーを出力しない。

## テスト方法

```bash
cd stock-screener
pytest tests/ -v
```

`tests/` はすべてAPIに依存しない単体テスト。新しい計算ロジックを追加した
場合は、欠損値の扱い（Noneが正しく伝播するか）を必ずテストに含めること。

## 変更時の注意

- `config/settings.py` の閾値・重みを変更する場合、意味と影響範囲をコメントで
  明記する。
- 新しいランキングカテゴリーを追加する場合は `src/ranking.py` の
  `CATEGORY_LABELS` と `build_categories()` の両方を更新し、
  `templates/rankings.html` にも反映する。
